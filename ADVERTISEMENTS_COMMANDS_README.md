# Автоматизация проверки статусов объявлений

Система включает в себя две команды для автоматического контроля жизненного цикла объявлений.

## Обзор команд

| Команда | Статус | Период | Срок задачи | Описание |
|---------|--------|--------|-------------|----------|
| `advertisements:check-reserved` | Резерв | 7 дней | 1 день | Проверка зарезервированных объявлений |
| `advertisements:check-active` | В продаже | 30 дней | 3 дня | Проверка активных объявлений |

## Быстрый старт

### Проверка всех команд

```bash
# Проверить зарезервированные объявления
php artisan advertisements:check-reserved

# Проверить активные объявления
php artisan advertisements:check-active

# Запустить обе команды последовательно
php artisan advertisements:check-reserved && php artisan advertisements:check-active
```

### Автоматический запуск

Обе команды настроены на **ежедневный автоматический запуск** через планировщик Laravel.

Для активации планировщика добавьте в cron:

```bash
* * * * * cd /path-to-your-project && php artisan schedule:run >> /dev/null 2>&1
```

## Детальное описание

### 1. Команда проверки резервов (7 дней)

**Команда:** `advertisements:check-reserved`

**Что проверяет:**
- Объявления в статусе "Резерв" (ID = 3)
- Которые не обновлялись 7 или более дней

**Что делает:**
- Переводит объявление в статус "Ревизия"
- Переводит связанный товар в статус "Ревизия"
- Создает задачи со сроком выполнения **1 день**
- Логирует все изменения

**Пример лога:**
```
Прошло 8 дней с момента резервирования данного объявления. 
Статус автоматически изменен на 'Ревизия'. 
Требуется актуализировать статус.
```

**Пример задачи:**
```
Прошло 8 дней с момента постановки товара в резерв. 
Требуется актуализация статуса.
```

📖 [Подробная документация](RESERVED_ADVERTISEMENTS_COMMAND.md)

---

### 2. Команда проверки активных объявлений (30 дней)

**Команда:** `advertisements:check-active`

**Что проверяет:**
- Объявления в статусе "В продаже" (ID = 2)
- Которые не обновлялись 30 или более дней

**Что делает:**
- Переводит объявление в статус "Ревизия"
- Переводит связанный товар в статус "Ревизия"
- Создает задачи со сроком выполнения **3 дня**
- Логирует все изменения

**Пример лога:**
```
Прошло 35 дней с момента публикации объявления в статусе 'В продаже'. 
Статус автоматически изменен на 'Ревизия'. 
Требуется актуализировать статус и информацию о товаре.
```

**Пример задачи:**
```
Прошло 35 дней с момента публикации товара. 
Требуется актуализация статуса, проверка наличия и актуальности информации.
```

📖 [Подробная документация](ACTIVE_ADVERTISEMENTS_COMMAND.md)

---

## Логика работы системы

```
┌─────────────────────────────────────────────────────────────────┐
│                     ЖИЗНЕННЫЙ ЦИКЛ ОБЪЯВЛЕНИЯ                   │
└─────────────────────────────────────────────────────────────────┘

    Создание объявления
           ↓
    ┌──────────────┐
    │  В продаже   │ ←── Активное объявление
    └──────────────┘
           │
           │ 30 дней без обновления
           ↓
    ┌──────────────┐
    │   Ревизия    │ ←── Требуется актуализация
    └──────────────┘
           │
           │ Менеджер актуализирует
           ↓
    ┌──────────────┐
    │  В продаже   │ или другой статус
    └──────────────┘

─────────────────────────────────────────────────────────────────

    Товар зарезервирован
           ↓
    ┌──────────────┐
    │    Резерв    │ ←── Товар зарезервирован
    └──────────────┘
           │
           │ 7 дней без обновления
           ↓
    ┌──────────────┐
    │   Ревизия    │ ←── Проверить сделку
    └──────────────┘
           │
           │ Менеджер проверяет
           ↓
    ┌──────────────┐     ┌──────────────┐
    │   Продано    │ или │  В продаже   │
    └──────────────┘     └──────────────┘
```

## Создаваемые сущности

### Логи (AdvLog / ProductLog)
- **Тип:** Системный
- **user_id:** null (от имени системы)
- **Содержание:** Причина изменения статуса

### Задачи (AdvAction / ProductAction)
- **Назначается:** Владельцу товара и создателю объявления
- **Статус:** false (не выполнена)
- **Срок:** 1-3 дня в зависимости от команды

## Мониторинг и отчетность

### Просмотр логов выполнения

После выполнения команды вы увидите детальный отчет:

```
Начало проверки объявлений в статусе "В продаже"...

Найдено объявлений для обработки: 5

Обработка объявления ID: 15 - Токарный станок
  ✓ Создана задача для менеджера ID: 5
  ✓ Создана задача для создателя объявления ID: 5
  ✓ Статусы успешно обновлены: В продаже → Ревизия
  ℹ Товар находился в продаже 35 дней

...

=== Результаты обработки ===
Всего найдено: 5
Успешно обработано: 5

Проверка завершена.
```

### Проверка задач менеджеров

Созданные задачи появятся в системе и будут видны:
- В карточке товара (раздел "Задачи")
- В карточке объявления (раздел "Задачи")
- В общем списке задач менеджера

## Настройка периодов

Если нужно изменить периоды проверки, отредактируйте соответствующие файлы:

### Изменить период для резерва (по умолчанию 7 дней)

Файл: `app/Console/Commands/CheckReservedAdvertisements.php`

```php
// Найдите строку:
$sevenDaysAgo = Carbon::now()->subDays(7);

// Измените на нужное количество дней, например 5:
$fiveDaysAgo = Carbon::now()->subDays(5);
```

### Изменить период для активных (по умолчанию 30 дней)

Файл: `app/Console/Commands/CheckActiveAdvertisements.php`

```php
// Найдите строку:
$thirtyDaysAgo = Carbon::now()->subDays(30);

// Измените на нужное количество дней, например 45:
$fortyFiveDaysAgo = Carbon::now()->subDays(45);
```

## Тестирование команд

### Тест в продакшн-режиме

```bash
# Просто запустите команду
php artisan advertisements:check-reserved
php artisan advertisements:check-active
```

### Проверка расписания

```bash
# Посмотреть список всех запланированных команд
php artisan schedule:list
```

Вывод должен содержать:
```
  0 0 * * *  php artisan advertisements:check-reserved ... Next Due: 12 hours from now
  0 0 * * *  php artisan advertisements:check-active ... Next Due: 12 hours from now
```

## Устранение неполадок

### Команда не выполняется автоматически

1. Проверьте, что cron настроен:
   ```bash
   crontab -l
   ```

2. Проверьте логи cron:
   ```bash
   grep CRON /var/log/syslog
   ```

3. Запустите планировщик вручную:
   ```bash
   php artisan schedule:run
   ```

### Статусы не найдены

Убедитесь, что в базе данных существуют нужные статусы:

```sql
-- Проверка статусов объявлений
SELECT * FROM advertisement_statuses WHERE name IN ('Резерв', 'Ревизия', 'В продаже');

-- Проверка статусов товаров
SELECT * FROM product_statuses WHERE name = 'Ревизия';

-- Проверка типов логов
SELECT * FROM log_types WHERE name = 'Системный';
```

### Команды не находятся

Очистите кеш и перезагрузите автозагрузчик:

```bash
php artisan clear-compiled
php artisan optimize:clear
composer dump-autoload
```

## Структура файлов

```
app/Console/
├── Kernel.php                              # Регистрация команд в планировщике
└── Commands/
    ├── CheckReservedAdvertisements.php    # Команда проверки резервов (7 дней)
    └── CheckActiveAdvertisements.php      # Команда проверки активных (30 дней)

docs/
├── ADVERTISEMENTS_COMMANDS_README.md       # Этот файл
├── RESERVED_ADVERTISEMENTS_COMMAND.md     # Детальная документация по резервам
└── ACTIVE_ADVERTISEMENTS_COMMAND.md       # Детальная документация по активным
```

## Лицензия и поддержка

Команды разработаны как часть системы управления объявлениями и товарами.
Для вопросов и предложений обращайтесь к команде разработки.

---

**Дата создания:** 2025  
**Версия:** 1.0

